PORT=3000
WEB_PASSWORD=change-me
# 服务器上推荐直接用环境变量传 cookie（不用文件）
NVT_COOKIE=
# 本机用文件时：
# NVT_COOKIE_FILE=./session-cookie.txt
# 采集间隔（秒），默认 10（分时走势 5 分 K 用，越短曲线越密；注意平台风控，被限流可调大）
POLL_INTERVAL=10
HISTORY_RETENTION_DAYS=90
# 若 nvtokens 需代理访问（如本机本地测试），填如 http://127.0.0.1:7897
PROXY=
